name: gitaction for terraform
on:
  workflow_dispatch:
  push:
    branches:
      - main
      - dev

jobs:
  my-pipeline:
    name: my-pipeline
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Display Files
        run: ls -ltra
